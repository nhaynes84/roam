tools/roam-send
build/
*.DS_Store
__pycache__/
**/__pycache__/
.venv/
**/.venv/
**/fp-info-cache
**/*-backups/
*.kicad_prl
housing/.venv/
housing/step/

# ROAM Touch hub: shared bearer token and the event database. The token is a
# secret; the database is machine state (and can contain transcripts).
roam-touch/hub/hub-token.txt
roam-touch/hub/bridge-state.json
roam-touch/hub/*.sqlite
roam-touch/hub/*.sqlite-wal
roam-touch/hub/*.sqlite-shm
.pytest_cache/

# Per-agent worktrees (EnterWorktree / manual `git worktree add`)
.claude/worktrees/
